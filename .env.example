# PocketBase Configuration
POCKETBASE_URL=http://localhost:8090
POCKETBASE_ADMIN_EMAIL=admin@example.com
POCKETBASE_ADMIN_PASSWORD=your-secure-password

# Next.js Configuration
NEXT_PUBLIC_POCKETBASE_URL=http://localhost:8090
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# Storage Configuration
# Backend type: 'local' or 's3' (default: local)
STORAGE_BACKEND=local

# Local Storage Configuration (required when STORAGE_BACKEND=local)
LOCAL_STORAGE_PATH=./data/uploads

# S3 Storage Configuration (required when STORAGE_BACKEND=s3)
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_FORCE_PATH_STYLE=false

# S3 Watcher Configuration (optional)
# ENABLE_S3_WATCHER=false
# S3_WATCH_PREFIX=uploads/
# S3_WATCH_WORKSPACE_ID=your-workspace-id
# S3_WATCH_INTERVAL_SECONDS=60
# S3_WATCH_REPROCESS_MODIFIED=false

# Google Cloud Configuration (optional - for Video Intelligence, Speech-to-Text, Transcoder)
# GOOGLE_PROJECT_ID=your-gcp-project-id
# Option 1: Inline JSON credentials (recommended for deployment)
# GOOGLE_CLOUD_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"..."}
# Option 2: Path to key file (good for local development)
# GOOGLE_CLOUD_KEY_FILE=/path/to/service-account-key.json
# Option 3: Omit both to use Application Default Credentials (gcloud auth)
# GCS Bucket for temporary file uploads (required when using local storage with Video Intelligence/Speech-to-Text)
# GCS_BUCKET=your-gcs-bucket-name
# ENABLE_GOOGLE_VIDEO_INTELLIGENCE=false
# ENABLE_GOOGLE_SPEECH=false
# ENABLE_GOOGLE_TRANSCODER=false

# Development
NODE_ENV=development
